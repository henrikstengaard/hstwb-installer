; Detect Amiga OS 3.2.1 Classes
; -----------------------------
;
; Date: 2021-12-29
; Author: Henrik Noerfjand Stengaard
;
; AmigaDOS script to detect valid amiga os 3.2.1 classes floppy disk or adf file.


; end, if volume name is not "Classes3.2.1:"
IF NOT "`Which $amigaosdisk:`" EQ "Classes3.2.1:"
  SKIP notvalid
ENDIF

; end, if ilbm datatype doesn't exist
IF NOT EXISTS "$amigaosdisk:Classes/Datatypes/ilbm.datatype.Z"
  SKIP notvalid
ENDIF

; end, if speedbar gadget doesn't exist
IF NOT EXISTS "$amigaosdisk:Classes/Gadgets/speedbar.gadget.Z"
  SKIP notvalid
ENDIF

; end, if window class doesn't exist
IF NOT EXISTS "$amigaosdisk:Classes/window.class.Z"
  SKIP notvalid
ENDIF

SKIP end


; not valid
; ---------
LAB notvalid

echo "Amiga OS 3.2.1 Classes is not valid"


; end
; ---

LAB end
