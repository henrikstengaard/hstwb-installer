; Demos WHDLoad Install Script
; --------------------------------
;
; Author: Henrik Noerfjand Stengaard
; Date: 2017-12-08
;
; An AmigaDOS script to install Demos WHDLoad sets from EAB WHDLoad Packs.


set installset `RequestChoice "Demos WHDLoad Set" "Select Demos WHDLoad set to install." "All|AGA|OCS|Skip"`

IF $installset EQ 1 VAL
  echo "All"
  execute USERPACKAGEDIR:Install/AGA
ENDIF

IF $installset EQ 2 VAL
  echo "AGA"
  execute USERPACKAGEDIR:Install/AGA
ENDIF

IF $installset EQ 3 VAL
  echo "OCS"
  execute USERPACKAGEDIR:Install/OCS
ENDIF