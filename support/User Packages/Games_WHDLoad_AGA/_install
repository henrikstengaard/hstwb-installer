; EAB WHDLoad Pack Install Script
; -------------------------------
;
; Author: Henrik Noerfjand Stengaard
; Date: 2017-11-23

; 'Games_WHDLoad_AGA' install script

; Install '0-9'
IF EXISTS "USERPACKAGEDIR:0-9"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "0-9"`"
  echo "Copying '0-9'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:0-9" "$itemdir" ALL
ENDIF

; Install '0-9.info'
IF EXISTS "USERPACKAGEDIR:0-9.info"
  Copy >NIL: "USERPACKAGEDIR:0-9.info" "$INSTALLDIR"
ENDIF

; Install 'A'
IF EXISTS "USERPACKAGEDIR:A"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "A"`"
  echo "Copying 'A'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:A" "$itemdir" ALL
ENDIF

; Install 'A.info'
IF EXISTS "USERPACKAGEDIR:A.info"
  Copy >NIL: "USERPACKAGEDIR:A.info" "$INSTALLDIR"
ENDIF

; Install 'B'
IF EXISTS "USERPACKAGEDIR:B"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "B"`"
  echo "Copying 'B'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:B" "$itemdir" ALL
ENDIF

; Install 'B.info'
IF EXISTS "USERPACKAGEDIR:B.info"
  Copy >NIL: "USERPACKAGEDIR:B.info" "$INSTALLDIR"
ENDIF

; Install 'C'
IF EXISTS "USERPACKAGEDIR:C"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "C"`"
  echo "Copying 'C'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:C" "$itemdir" ALL
ENDIF

; Install 'C.info'
IF EXISTS "USERPACKAGEDIR:C.info"
  Copy >NIL: "USERPACKAGEDIR:C.info" "$INSTALLDIR"
ENDIF

; Install 'D'
IF EXISTS "USERPACKAGEDIR:D"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "D"`"
  echo "Copying 'D'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:D" "$itemdir" ALL
ENDIF

; Install 'D.info'
IF EXISTS "USERPACKAGEDIR:D.info"
  Copy >NIL: "USERPACKAGEDIR:D.info" "$INSTALLDIR"
ENDIF

; Install 'E'
IF EXISTS "USERPACKAGEDIR:E"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "E"`"
  echo "Copying 'E'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:E" "$itemdir" ALL
ENDIF

; Install 'E.info'
IF EXISTS "USERPACKAGEDIR:E.info"
  Copy >NIL: "USERPACKAGEDIR:E.info" "$INSTALLDIR"
ENDIF

; Install 'F'
IF EXISTS "USERPACKAGEDIR:F"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "F"`"
  echo "Copying 'F'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:F" "$itemdir" ALL
ENDIF

; Install 'F.info'
IF EXISTS "USERPACKAGEDIR:F.info"
  Copy >NIL: "USERPACKAGEDIR:F.info" "$INSTALLDIR"
ENDIF

; Install 'G'
IF EXISTS "USERPACKAGEDIR:G"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "G"`"
  echo "Copying 'G'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:G" "$itemdir" ALL
ENDIF

; Install 'G.info'
IF EXISTS "USERPACKAGEDIR:G.info"
  Copy >NIL: "USERPACKAGEDIR:G.info" "$INSTALLDIR"
ENDIF

; Install 'H'
IF EXISTS "USERPACKAGEDIR:H"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "H"`"
  echo "Copying 'H'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:H" "$itemdir" ALL
ENDIF

; Install 'H.info'
IF EXISTS "USERPACKAGEDIR:H.info"
  Copy >NIL: "USERPACKAGEDIR:H.info" "$INSTALLDIR"
ENDIF

; Install 'I'
IF EXISTS "USERPACKAGEDIR:I"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "I"`"
  echo "Copying 'I'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:I" "$itemdir" ALL
ENDIF

; Install 'I.info'
IF EXISTS "USERPACKAGEDIR:I.info"
  Copy >NIL: "USERPACKAGEDIR:I.info" "$INSTALLDIR"
ENDIF

; Install 'J'
IF EXISTS "USERPACKAGEDIR:J"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "J"`"
  echo "Copying 'J'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:J" "$itemdir" ALL
ENDIF

; Install 'J.info'
IF EXISTS "USERPACKAGEDIR:J.info"
  Copy >NIL: "USERPACKAGEDIR:J.info" "$INSTALLDIR"
ENDIF

; Install 'K'
IF EXISTS "USERPACKAGEDIR:K"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "K"`"
  echo "Copying 'K'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:K" "$itemdir" ALL
ENDIF

; Install 'K.info'
IF EXISTS "USERPACKAGEDIR:K.info"
  Copy >NIL: "USERPACKAGEDIR:K.info" "$INSTALLDIR"
ENDIF

; Install 'L'
IF EXISTS "USERPACKAGEDIR:L"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "L"`"
  echo "Copying 'L'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:L" "$itemdir" ALL
ENDIF

; Install 'L.info'
IF EXISTS "USERPACKAGEDIR:L.info"
  Copy >NIL: "USERPACKAGEDIR:L.info" "$INSTALLDIR"
ENDIF

; Install 'M'
IF EXISTS "USERPACKAGEDIR:M"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "M"`"
  echo "Copying 'M'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:M" "$itemdir" ALL
ENDIF

; Install 'M.info'
IF EXISTS "USERPACKAGEDIR:M.info"
  Copy >NIL: "USERPACKAGEDIR:M.info" "$INSTALLDIR"
ENDIF

; Install 'N'
IF EXISTS "USERPACKAGEDIR:N"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "N"`"
  echo "Copying 'N'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:N" "$itemdir" ALL
ENDIF

; Install 'N.info'
IF EXISTS "USERPACKAGEDIR:N.info"
  Copy >NIL: "USERPACKAGEDIR:N.info" "$INSTALLDIR"
ENDIF

; Install 'O'
IF EXISTS "USERPACKAGEDIR:O"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "O"`"
  echo "Copying 'O'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:O" "$itemdir" ALL
ENDIF

; Install 'O.info'
IF EXISTS "USERPACKAGEDIR:O.info"
  Copy >NIL: "USERPACKAGEDIR:O.info" "$INSTALLDIR"
ENDIF

; Install 'P'
IF EXISTS "USERPACKAGEDIR:P"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "P"`"
  echo "Copying 'P'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:P" "$itemdir" ALL
ENDIF

; Install 'P.info'
IF EXISTS "USERPACKAGEDIR:P.info"
  Copy >NIL: "USERPACKAGEDIR:P.info" "$INSTALLDIR"
ENDIF

; Install 'Q'
IF EXISTS "USERPACKAGEDIR:Q"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "Q"`"
  echo "Copying 'Q'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:Q" "$itemdir" ALL
ENDIF

; Install 'Q.info'
IF EXISTS "USERPACKAGEDIR:Q.info"
  Copy >NIL: "USERPACKAGEDIR:Q.info" "$INSTALLDIR"
ENDIF

; Install 'R'
IF EXISTS "USERPACKAGEDIR:R"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "R"`"
  echo "Copying 'R'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:R" "$itemdir" ALL
ENDIF

; Install 'R.info'
IF EXISTS "USERPACKAGEDIR:R.info"
  Copy >NIL: "USERPACKAGEDIR:R.info" "$INSTALLDIR"
ENDIF

; Install 'S'
IF EXISTS "USERPACKAGEDIR:S"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "S"`"
  echo "Copying 'S'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:S" "$itemdir" ALL
ENDIF

; Install 'S.info'
IF EXISTS "USERPACKAGEDIR:S.info"
  Copy >NIL: "USERPACKAGEDIR:S.info" "$INSTALLDIR"
ENDIF

; Install 'T'
IF EXISTS "USERPACKAGEDIR:T"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "T"`"
  echo "Copying 'T'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:T" "$itemdir" ALL
ENDIF

; Install 'T.info'
IF EXISTS "USERPACKAGEDIR:T.info"
  Copy >NIL: "USERPACKAGEDIR:T.info" "$INSTALLDIR"
ENDIF

; Install 'U'
IF EXISTS "USERPACKAGEDIR:U"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "U"`"
  echo "Copying 'U'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:U" "$itemdir" ALL
ENDIF

; Install 'U.info'
IF EXISTS "USERPACKAGEDIR:U.info"
  Copy >NIL: "USERPACKAGEDIR:U.info" "$INSTALLDIR"
ENDIF

; Install 'V'
IF EXISTS "USERPACKAGEDIR:V"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "V"`"
  echo "Copying 'V'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:V" "$itemdir" ALL
ENDIF

; Install 'V.info'
IF EXISTS "USERPACKAGEDIR:V.info"
  Copy >NIL: "USERPACKAGEDIR:V.info" "$INSTALLDIR"
ENDIF

; Install 'W'
IF EXISTS "USERPACKAGEDIR:W"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "W"`"
  echo "Copying 'W'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:W" "$itemdir" ALL
ENDIF

; Install 'W.info'
IF EXISTS "USERPACKAGEDIR:W.info"
  Copy >NIL: "USERPACKAGEDIR:W.info" "$INSTALLDIR"
ENDIF

; Install 'X'
IF EXISTS "USERPACKAGEDIR:X"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "X"`"
  echo "Copying 'X'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:X" "$itemdir" ALL
ENDIF

; Install 'X.info'
IF EXISTS "USERPACKAGEDIR:X.info"
  Copy >NIL: "USERPACKAGEDIR:X.info" "$INSTALLDIR"
ENDIF

; Install 'Y.info'
IF EXISTS "USERPACKAGEDIR:Y.info"
  Copy >NIL: "USERPACKAGEDIR:Y.info" "$INSTALLDIR"
ENDIF

; Install 'Z'
IF EXISTS "USERPACKAGEDIR:Z"
  set itemdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "Z"`"
  echo "Copying 'Z'..."
  IF NOT EXISTS "$itemdir"
    MakePath >NIL: "$itemdir"
  ENDIF
  Copy >NIL: "USERPACKAGEDIR:Z" "$itemdir" ALL
ENDIF

; Install 'Z.info'
IF EXISTS "USERPACKAGEDIR:Z.info"
  Copy >NIL: "USERPACKAGEDIR:Z.info" "$INSTALLDIR"
ENDIF