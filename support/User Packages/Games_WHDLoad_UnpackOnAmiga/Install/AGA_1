; 'B' index directory
echo "Installing 'B'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "B"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:B.info"
  Copy >NIL: "USERPACKAGEDIR:B.info" "$INSTALLDIR"
ENDIF

; Extract 'Battlestorm' entry archive
IF EXISTS "USERPACKAGEDIR:Battlestorm_v1.2_2008.zip"
  unzip -qq -o -x "USERPACKAGEDIR:Battlestorm_v1.2_2008.zip" -d "$indexdir"
ENDIF

; Extract 'BattlestormCDTV' entry archive
IF EXISTS "USERPACKAGEDIR:Battlestorm_v1.2_CDTV.zip"
  unzip -qq -o -x "USERPACKAGEDIR:Battlestorm_v1.2_CDTV.zip" -d "$indexdir"
ENDIF

; 'D' index directory
echo "Installing 'D'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "D"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:D.info"
  Copy >NIL: "USERPACKAGEDIR:D.info" "$INSTALLDIR"
ENDIF

; Extract 'DinoWars' entry archive
IF EXISTS "USERPACKAGEDIR:DinoWars_v1.1.lha"
  lha -q -m1 x "USERPACKAGEDIR:DinoWars_v1.1.lha" "$indexdir/"
ENDIF

; Extract 'DinoWarsDe' entry archive
IF EXISTS "USERPACKAGEDIR:DinoWars_v1.1_De.lha"
  lha -q -m1 x "USERPACKAGEDIR:DinoWars_v1.1_De.lha" "$indexdir/"
ENDIF

; 'E' index directory
echo "Installing 'E'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "E"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:E.info"
  Copy >NIL: "USERPACKAGEDIR:E.info" "$INSTALLDIR"
ENDIF

; Extract 'EmeraldMinesCD' entry archive
IF EXISTS "USERPACKAGEDIR:EmeraldMines_v1.0_CD.lha"
  lha -q -m1 x "USERPACKAGEDIR:EmeraldMines_v1.0_CD.lha" "$indexdir/"
ENDIF

; 'F' index directory
echo "Installing 'F'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "F"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:F.info"
  Copy >NIL: "USERPACKAGEDIR:F.info" "$INSTALLDIR"
ENDIF

; Extract 'FastLane' entry archive
IF EXISTS "USERPACKAGEDIR:FastLane_v1.0.lha"
  lha -q -m1 x "USERPACKAGEDIR:FastLane_v1.0.lha" "$indexdir/"
ENDIF

; Extract 'FlipFlop' entry archive
IF EXISTS "USERPACKAGEDIR:FlipFlop_v1.0.zip"
  unzip -qq -o -x "USERPACKAGEDIR:FlipFlop_v1.0.zip" -d "$indexdir"
ENDIF

; Extract 'FussballTotalDe' entry archive
IF EXISTS "USERPACKAGEDIR:FussballTotal_v1.1_De_2135.lha"
  lha -q -m1 x "USERPACKAGEDIR:FussballTotal_v1.1_De_2135.lha" "$indexdir/"
ENDIF

; 'H' index directory
echo "Installing 'H'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "H"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:H.info"
  Copy >NIL: "USERPACKAGEDIR:H.info" "$INSTALLDIR"
ENDIF

; Extract 'HiredGuns1MB' entry archive
IF EXISTS "USERPACKAGEDIR:HiredGuns_v1.0_1MB_1114.zip"
  unzip -qq -o -x "USERPACKAGEDIR:HiredGuns_v1.0_1MB_1114.zip" -d "$indexdir"
ENDIF

; Extract 'HiredGuns512KB' entry archive
IF EXISTS "USERPACKAGEDIR:HiredGuns_v1.0_512KB_1114.zip"
  unzip -qq -o -x "USERPACKAGEDIR:HiredGuns_v1.0_512KB_1114.zip" -d "$indexdir"
ENDIF

; Extract 'HoleInOne' entry archive
IF EXISTS "USERPACKAGEDIR:HoleInOne_v1.1.lha"
  lha -q -m1 x "USERPACKAGEDIR:HoleInOne_v1.1.lha" "$indexdir/"
ENDIF

; Extract 'HoleInOne&DataDisks' entry archive
IF EXISTS "USERPACKAGEDIR:HoleInOne&DataDisks_v1.1.lha"
  lha -q -m1 x "USERPACKAGEDIR:HoleInOne&DataDisks_v1.1.lha" "$indexdir/"
ENDIF

; 'M' index directory
echo "Installing 'M'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "M"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:M.info"
  Copy >NIL: "USERPACKAGEDIR:M.info" "$INSTALLDIR"
ENDIF

; Extract 'MightAndMagic3De' entry archive
IF EXISTS "USERPACKAGEDIR:MightAndMagic3_v1.2_De.lha"
  lha -q -m1 x "USERPACKAGEDIR:MightAndMagic3_v1.2_De.lha" "$indexdir/"
ENDIF

; 'O' index directory
echo "Installing 'O'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "O"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:O.info"
  Copy >NIL: "USERPACKAGEDIR:O.info" "$INSTALLDIR"
ENDIF

; Extract 'OsWALDOfTheIceFloes' entry archive
IF EXISTS "USERPACKAGEDIR:OsWALDOfTheIceFloes_v1.0.zip"
  unzip -qq -o -x "USERPACKAGEDIR:OsWALDOfTheIceFloes_v1.0.zip" -d "$indexdir"
ENDIF

; 'P' index directory
echo "Installing 'P'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "P"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:P.info"
  Copy >NIL: "USERPACKAGEDIR:P.info" "$INSTALLDIR"
ENDIF

; Extract 'PizzaConnectionDe' entry archive
IF EXISTS "USERPACKAGEDIR:PizzaConnection_v1.0_De.lha"
  lha -q -m1 x "USERPACKAGEDIR:PizzaConnection_v1.0_De.lha" "$indexdir/"
ENDIF

; 'S' index directory
echo "Installing 'S'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "S"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:S.info"
  Copy >NIL: "USERPACKAGEDIR:S.info" "$INSTALLDIR"
ENDIF

; Extract 'SimLife' entry archive
IF EXISTS "USERPACKAGEDIR:SimLife_v1.0_2585.zip"
  unzip -qq -o -x "USERPACKAGEDIR:SimLife_v1.0_2585.zip" -d "$indexdir"
ENDIF

; Extract 'SimLifeAGA' entry archive
IF EXISTS "USERPACKAGEDIR:SimLife_v1.0_AGA_0889.zip"
  unzip -qq -o -x "USERPACKAGEDIR:SimLife_v1.0_AGA_0889.zip" -d "$indexdir"
ENDIF

; Extract 'SimLifeDe' entry archive
IF EXISTS "USERPACKAGEDIR:SimLife_v1.0_De_0576.zip"
  unzip -qq -o -x "USERPACKAGEDIR:SimLife_v1.0_De_0576.zip" -d "$indexdir"
ENDIF

; Extract 'SkateOfTheArt' entry archive
IF EXISTS "USERPACKAGEDIR:SkateOfTheArt_v1.0.zip"
  unzip -qq -o -x "USERPACKAGEDIR:SkateOfTheArt_v1.0.zip" -d "$indexdir"
ENDIF

; Extract 'Sorefist' entry archive
IF EXISTS "USERPACKAGEDIR:Sorefist_v1.0.zip"
  unzip -qq -o -x "USERPACKAGEDIR:Sorefist_v1.0.zip" -d "$indexdir"
ENDIF

; 'T' index directory
echo "Installing 'T'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "T"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:T.info"
  Copy >NIL: "USERPACKAGEDIR:T.info" "$INSTALLDIR"
ENDIF

; Extract 'TransworldDe' entry archive
IF EXISTS "USERPACKAGEDIR:Transworld_v1.0_De_2837.lha"
  lha -q -m1 x "USERPACKAGEDIR:Transworld_v1.0_De_2837.lha" "$indexdir/"
ENDIF

; Extract 'TrivialPursuit' entry archive
IF EXISTS "USERPACKAGEDIR:TrivialPursuit_v1.2.zip"
  unzip -qq -o -x "USERPACKAGEDIR:TrivialPursuit_v1.2.zip" -d "$indexdir"
ENDIF

; Extract 'TrivialPursuitDe' entry archive
IF EXISTS "USERPACKAGEDIR:TrivialPursuit_v1.2_De_0595.zip"
  unzip -qq -o -x "USERPACKAGEDIR:TrivialPursuit_v1.2_De_0595.zip" -d "$indexdir"
ENDIF

; Extract 'TrivialPursuitFr' entry archive
IF EXISTS "USERPACKAGEDIR:TrivialPursuit_v1.2_Fr_2147.zip"
  unzip -qq -o -x "USERPACKAGEDIR:TrivialPursuit_v1.2_Fr_2147.zip" -d "$indexdir"
ENDIF

; 'W' index directory
echo "Installing 'W'..."
set indexdir "`execute INSTALLDIR:S/CombinePath "$INSTALLDIR" "W"`"
IF NOT EXISTS "$indexdir"
  MakePath >NIL: "$indexdir"
ENDIF
IF EXISTS "USERPACKAGEDIR:W.info"
  Copy >NIL: "USERPACKAGEDIR:W.info" "$INSTALLDIR"
ENDIF

; Extract 'WPotrzaskuPl' entry archive
IF EXISTS "USERPACKAGEDIR:WPotrzasku_v1.0_Pl.zip"
  unzip -qq -o -x "USERPACKAGEDIR:WPotrzasku_v1.0_Pl.zip" -d "$indexdir"
ENDIF
